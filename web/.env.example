# Настройки фронтенда (скопировать в .env при необходимости).
# Адрес API-сервера. Локально по умолчанию http://localhost:4000 — менять не нужно.
# В проде фронтенд и API живут на одном домене, поэтому оставляем пустым:
# VITE_API_URL=
